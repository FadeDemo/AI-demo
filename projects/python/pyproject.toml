[project]
name = "python-course"
version = "0.1.0"
description = "Runnable examples for the Python course notes"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = [
    "ruff>=0.15.22",
]
